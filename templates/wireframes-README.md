# {{APP_NAME}} — Wireframes

> Written by `/plan-wireframes` (drives a Figma MCP). Low-fidelity frames per
> feature so the app's flow is clear before the dev stage. **Part of Alex's
> approval gate** with the implementation guide.

**Figma file/project:** _link_
**Updated:** {{DATE}}

## Screen → feature map

| Feature | Screen | Frame link | States covered |
|---------|--------|------------|----------------|
| _auth_ | Login | _link_ | default, error |
| _auth_ | Sign up | _link_ | default, loading, error |
| _…_ | _…_ | _…_ | _…_ |

## Primary user flow(s)

1. _Landing → Sign up → Onboarding → <core screen> → <core job>_
2. _…_

## Notes

- Low-fidelity: structure and flow, not pixel polish.
- Non-happy states (empty / loading / error) are included — that's where flows
  break.
- If `docs/DESIGN.md` exists, these honor its tokens.
